spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: user-service
              uri: lb://user-service
              predicates:
                  - Path=/users/**,/auth/**
            - id: product-service
              uri: lb://product-service
              predicates:
                    - Path=/products/**
            - id: cart-service
              uri: lb://cart-service
              predicates:
                  - Path=/carts/**,/cart-items/**
            - id: order-service
              uri: lb://order-service
              predicates:
                  - Path=/orders/**
            - id: payment-service
              uri: lb://payment-service
              predicates:
                  - Path=/payments/**
    discovery:
      locator:
        enabled: true
        lower-case-service-id: true
  application:
    name: api-gateway
server:
  port: 8080
eureka:
    client:
      service-url:
            defaultZone: http://eureka:8761/eureka/
      register-with-eureka: true
      fetch-registry: true
jwt:
  secret: ${JWT_SECRET:39616e41306663523363363346526134}
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.web: DEBUG